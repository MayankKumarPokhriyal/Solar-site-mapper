/**
 * Phase 2 hook: export a list of default addresses to preload when you receive the final assessment sites.
 * Keep this empty in phase 1 so the app stays fully dynamic.
 *
 * Example later:
 * export const INITIAL_SITE_ADDRESSES = [
 *   "123 Main St, Austin, TX",
 * ];
 */
export const INITIAL_SITE_ADDRESSES = [];
